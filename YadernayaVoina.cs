using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // ”ничтожить €дерную войну
    void Start()
    {
        Destroy(gameObject);
        print("Destroy.cs.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
